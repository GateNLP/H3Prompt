
services:
  h3prompt:
    image: elg.docker.gate.ac.uk/h3prompt:latest
    restart: always
    ports:
      - 8169:8000
    volumes:
#      - ./checkpoint-2470:/tmp/model
      - /data3/gate/cache/transformers/:/home/mambauser/.cache/huggingface/hub
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['2']
            capabilities: [gpu]
